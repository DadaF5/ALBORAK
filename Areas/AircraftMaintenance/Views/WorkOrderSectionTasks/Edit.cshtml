@model FRAProject.ViewModels.AircraftMaintenance.WorkOrderSectionTaskFormViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Modifier le travail";
}

<div class="card card-outline card-primary">
    <div class="card-header"><h3 class="card-title">Modifier le travail effectué</h3></div>
    <form asp-action="Edit" asp-route-id="@Model.Id" method="post">
        <div class="card-body">@await Html.PartialAsync("_Form", Model)</div>
        <div class="card-footer">
            <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Enregistrer</button>
            <a asp-action="Index" asp-route-id="@Model.WorkOrderSectionId" class="btn btn-default">Annuler</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

}