@model FRAProject.ViewModels.AircraftMaintenance.WorkOrderSectionFormViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Nouvelle section";
}

<div class="card card-outline card-primary">
    <div class="card-header"><h3 class="card-title">Nouvelle section (Formule 13)</h3></div>
    <form asp-action="Create" method="post">
        <div class="card-body">@await Html.PartialAsync("_Form", Model)</div>
        <div class="card-footer">
            <button type="submit" class="btn btn-primary"><i class="fas fa-save"></i> Enregistrer</button>
            <a asp-action="Index" asp-route-id="@Model.WorkOrderId" class="btn btn-default">Annuler</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

}