@{
    // NEW (Batch 10) — see OdvPlanning/_ViewStart.cshtml for the full note
    // on why a subfolder-level file is safe to add regardless of any
    // parent _ViewStart.cshtml.
    Layout = "_SquadronOpsSettingsLayout";
}
